name: typescript-native-crypto

on:
  push:
    paths:
      - "typescript/native/**"
      - ".github/workflows/typescript-native.yml"
  pull_request:
    paths:
      - "typescript/native/**"
      - ".github/workflows/typescript-native.yml"
  workflow_dispatch:
  release:
    types: [published]

jobs:
  check:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [20]
    defaults:
      run:
        working-directory: typescript/native
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - uses: dtolnay/rust-toolchain@stable
      - run: npm install
      - run: rustc --version
      - run: cargo check
      - run: npm run build:native
      - uses: actions/upload-artifact@v4
        with:
          name: dilithium-sdk-node-crypto-${{ matrix.os }}
          path: |
            typescript/native/*.node
            typescript/native/*.tgz

  package:
    if: github.event_name != 'pull_request'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    defaults:
      run:
        working-directory: typescript/native
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
      - uses: dtolnay/rust-toolchain@stable
      - run: npm install
      - run: npm run build:native
      - run: npm pack
      - uses: actions/upload-artifact@v4
        with:
          name: dilithium-sdk-node-crypto-package-${{ matrix.os }}
          path: typescript/native/*.tgz

  publish:
    if: github.event_name == 'release'
    needs: [check, package]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: typescript/native
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
      - uses: dtolnay/rust-toolchain@stable
      - run: npm install
      - run: npm run build:native
      - run: npm publish --provenance --access public
